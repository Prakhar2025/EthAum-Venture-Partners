def fetch_trust_signals(url: str) -> dict:
    """
    Fetch verification signals for a given URL (e.g. domain age, social stats).
    """
    return {
        "domain_age_years": 0,
        "linkedin_count": 0
    }
