services:
  - type: web
    name: ethaum-ai-backend
    runtime: python
    rootDir: ethaum-ai/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
    plan: free
